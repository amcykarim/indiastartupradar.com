(function(){const f=document.querySelector('[data-break-even-form]');if(!f)return;const box=document.querySelector('[data-result]'),units=document.querySelector('[data-units]'),revenue=document.querySelector('[data-revenue]'),detail=document.querySelector('[data-detail]');const money=new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0});f.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(f),fixed=Number(d.get('fixed')),price=Number(d.get('price')),variable=Number(d.get('variable')),contribution=price-variable;box.hidden=false;if(![fixed,price,variable].every(Number.isFinite)||fixed<0||price<=0||variable<0||contribution<=0){units.textContent='Check your inputs';revenue.textContent='';detail.textContent='Selling price must be greater than variable cost, and all values must be valid non-negative numbers.';box.focus();return}const count=Math.ceil(fixed/contribution);units.textContent=count.toLocaleString('en-IN')+' units';revenue.textContent='Break-even revenue: '+money.format(count*price);detail.textContent='Contribution per unit: '+money.format(contribution)+'. Units are rounded up.';box.focus()});f.addEventListener('reset',()=>{box.hidden=true;units.textContent='';revenue.textContent='';detail.textContent=''})}());
